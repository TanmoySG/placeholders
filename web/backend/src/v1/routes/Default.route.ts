// import express, { type Router } from "express";
// import { defaultController } from "../controllers";

// const router: Router = express.Router();

// router.get("/", defaultController.poc);

// export default router;
